import Link from "@/components/NoPrefetchLink";

export default function SecurityPage() {
  return <main className="legal-page" id="main-content"><article className="legal-shell"><Link className="brand" href="/"><span className="brand-mark" />KeepMe</Link><span className="section-kicker">Security</span><h1>Protection throughout the session</h1><p>KeepMe uses opaque session capabilities, organization-scoped retailer access, private no-store image delivery, strict upload decoding, metadata removal, rate limits, provider budgets, encrypted object storage, and verified deletion.</p><h2>Production controls</h2><p>Live personal-image processing fails closed unless database, object-storage encryption, authentication, receipt-signing, cleanup, malware-scanning, and integrity-service settings are present. The controlled synthetic demo remains available, and sensitive values stay server-only.</p><h2>Receipts and audit events</h2><p>Integrity receipts contain digests of the approved contract and result and are signed. Operational logs exclude images, secrets, source filenames, provider result URLs, and direct identity attributes.</p><h2>Report a concern</h2><p>Do not include personal images, credentials, or exploit details in public reports. Use the security contact configured by the retailer operating this deployment.</p></article></main>;
}
